---
name: Tests

on:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    name: Tests
    strategy:
      fail-fast: false

    steps:
      - uses: actions/checkout@v4
      - name: Run Tests
        run: |
          docker compose run test
