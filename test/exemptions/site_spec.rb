RSpec.describe "Exemptions configured at the server/module level" do
  # These are paths/URLs marked as exempt in the site or virtual host config.
  # They should bypass enforcement and delegation.
  #
  # So these should only require apache config?
  # Do we have examples of what this looks like?
end
